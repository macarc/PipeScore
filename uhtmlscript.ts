/*import { render } from 'uhtml';
import Score, { ScoreModel } from './Score';

const updateScore = (newScore: ScoreModel) => render(document.body, Score.render(newScore, { updateScore }));

updateScore(Score.init());
*/
